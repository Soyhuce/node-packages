import { useIntersectionObserver } from './useIntersectionObserver'

describe('react-utils.hooks.useIntersectionObserver', () => {
  it('should be defined', () => {
    expect(useIntersectionObserver).toBeDefined()
  })
})
