import { useIsomorphicLayoutEffect } from './useIsomorphicLayoutEffect'

describe('react-utils.hooks.useIsomorphicLayoutEffect', () => {
  it('should be defined', () => {
    expect(useIsomorphicLayoutEffect).toBeDefined()
  })
})
